- if @escolas_count > 0
  %table.table.table-striped
    %thead
      %tr
        %th Nome
        %th Município
        %th Desde
        %th
    %tbody
      - @escolas.each do |escola|
        %tr
          %td= link_to escola.nome, escola_path(escola)
          %td= escola.cidade.nome
          %td= formata_data escola.created_at
          %td{:style=>"width:85px"}
            %a{:href=>edit_escola_path(escola), :class => 'btn btn-primary'} 
              %i.icon-edit

            = link_to '<i class="icon-trash"></i>'.html_safe, escola_path(escola), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Quer mesmo excluir a Escola e todos seus dados?')) }, :class => 'btn btn-danger'

= link_to 'Nova', new_escola_path, :class => 'btn btn-primary'